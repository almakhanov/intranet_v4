<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/navigation_student_profile"
            android:iconTint="#505050"
            android:icon="@drawable/ic_profile"
            android:checked="true"
            android:title="Profile" />
        <item
            android:id="@+id/navigation_student_course"
            android:iconTint="#505050"
            android:icon="@drawable/ic_folder"
            android:title="Courses"  />
    </group>

    <item android:title="Action">
        <menu>
            <item
                android:id="@+id/navigation_student_about"
                android:iconTint="#505050"
                android:icon="@drawable/ic_about"
                android:title="About" />
            <item
                android:id="@+id/navigation_student_logout"
                android:icon="@drawable/ic_sign_out"
                android:iconTint="#505050"
                android:title="Sign out"  />
        </menu>
    </item>

</menu>