<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kz.batana.intranet_v4">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/logo_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.sign_in.SignInActivity" />
        <activity android:name=".ui.sign_in.sign_up.SignUpActivity" />
        <activity android:name=".ui.student_page.StudentMainActivity" />
        <activity android:name=".ui.admin_page.AdminMainActivity" />
        <activity android:name=".ui.teacher_page.TeacherMainActivity" />
        <activity
            android:name=".ui.testing_page.TestActivity"
            android:label="@string/title_activity_test"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.teacher_page.courses.course_add.CourseAddActivity"></activity>
    </application>

</manifest>